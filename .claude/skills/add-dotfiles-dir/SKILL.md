---
name: add-dotfiles-dir
description: ホームディレクトリのドットディレクトリをディレクトリごとシンボリックリンク方式でdotfiles管理に追加する。「~/.fooをディレクトリごと管理したい」「~/.fooをdotfilesに追加して（ディレクトリごと）」等と言われたときに使う。個別ファイルのみの管理には使わない。
argument-hint: "[ディレクトリパス（例: ~/.agents）]"
---

# add-dotfiles-dir

ホームディレクトリのドットディレクトリをdotfiles管理（ディレクトリごとシンボリックリンク方式）に追加する。

## 前提知識

ディレクトリごとシンボリックリンク方式では、`dot_foo` ディレクトリが `~/.foo` そのものになる。つまり：
- **git管理しないファイルも含めて、元ディレクトリの中身すべてが `dot_foo/` に存在する必要がある**
- `.gitignore` の許可リスト方式（`*` で全除外 → `!` で管理対象のみ許可）でgit追跡対象を制御する

## 手順

### 1. 元ディレクトリの中身を確認

`$ARGUMENTS` の中身を一覧表示し、ユーザーに以下を確認する：
- git管理対象にするファイル/ディレクトリはどれか

### 2. dot_ディレクトリ作成

元ディレクトリの中身をまるごとコピーする（rsync等を使用）。

```
rsync -a ~/.foo/ dot_foo/
```

### 3. .gitignore作成

`dot_foo/.gitignore` を許可リスト方式で作成する。`dot_claude/.gitignore` を参考にすること。

```
*
!.gitignore
!管理対象ファイル1
!管理対象ファイル2
```

サブディレクトリ内にgit管理しないファイルがある場合は、そのサブディレクトリにも `.gitignore` を作成する。

### 4. install.sh更新

`install.sh` の「ディレクトリごとシンボリックリンク」セクションに追加する。

```
ln -sfn "$(pwd)/dot_foo" ~/.foo
```

### 5. シンボリックリンクの適用

install.shに追記するだけでなく、**実際にシンボリックリンクを作成する**。

⚠️ **`ln -sfn` の注意点**: 対象パスに既存のディレクトリがある場合、`ln -sfn` はディレクトリを置き換えず、**ディレクトリの中にリンクを作ってしまう**。必ず先に既存ディレクトリを削除してからリンクを作成すること。

```bash
# 既存ディレクトリを削除してからリンク作成
rm -rf ~/.foo
ln -sfn "$(pwd)/dot_foo" ~/.foo
```

削除前に、元ディレクトリの中身が `dot_foo/` にすべてコピー済みであることを確認すること。

### 6. 報告

変更内容をまとめて報告する（作成したファイル、git管理対象、gitignore対象）。
